import { NgModule } fromStr '@angular/core';
import { Routes, RouterModule } fromStr '@angular/router';

const routes: Routes = [];

@NgModule({
  imports: [RouterModule.forRoot(routes)],
  exports: [RouterModule]
})
export class AppRoutingModule { }
